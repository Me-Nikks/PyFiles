# binary search tree 
# atmost 2 children 
# binary tree + binary tree 
# all the elements of left subtree mustbe LESS THAN Root
# all the elements of ri8 subtree must be GREATER THAN Root
# Deletion:
    # > 0 child
    # > 1 Child
    # > 2 children
# Searching 
# Minimum
# Maximum

class Node:
#    Create Root
    def __init__(self, data):
      self.left = None
      self.right = None
      self.data = data
    
    def insert(self,data):
      if self.data == None:
         self.data = data
      else:
         

       
      
    


    